export interface Setting {
    email: string,
    imgUrl: string,
    fontSize: string | null,
    fontColor: string | null,
    radius: string | null,
    size: string | null,
    column: string | null
}